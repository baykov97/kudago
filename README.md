# KudaGo Events Platform

Профессиональное веб-приложение для просмотра событий из [KudaGo API](https://docs.kudago.com/api/) с полной поддержкой Server-Side Rendering (SSR), SEO-оптимизацией и мультигородской архитектурой. Построено на **Nuxt 4** с использованием современных практик разработки.

## 🎯 О проекте

Проект демонстрирует современный подход к разработке production-ready приложений на Nuxt 4 с акцентом на:
- **Server-Side Rendering (SSR)** для улучшения SEO и производительности
- **SEO-оптимизацию** с динамическими метатегами и sitemap
- **Архитектурные паттерны** для масштабируемости и поддерживаемости
- **Оптимизацию производительности** через кеширование и lazy loading

## 🛠 Технологический стек

### Core
- **Nuxt 4.1+** — фреймворк на базе Vue 3 с SSR/SSG
- **Vue 3.5** — Composition API, `<script setup>`
- **TypeScript** — типизация для надежности кода
- **Pinia** — state management

### Модули и инструменты
- **@nuxtjs/sitemap** — динамическая генерация sitemap.xml
- **@nuxtjs/robots** — управление robots.txt
- **Sass/SCSS** — препроцессор стилей
- **Nitro** — серверный движок Nuxt

### API
- **KudaGo Public API v1.4** — источник данных о событиях

## ✨ Ключевые особенности

### 1. Server-Side Rendering (SSR)
- Все страницы генерируются на сервере
- Готовый HTML отдается при первом запросе
- Оптимизация для поисковых систем

### 2. Мультигородская архитектура
- Динамическая маршрутизация: `/[city]/events`, `/[city]/events/[id]`
- Глобальный middleware для валидации городов
- Синхронизация выбранного города через cookie
- Fallback механизмы при ошибках API

### 3. SEO-оптимизация
- Динамические метатеги (title, description)
- OpenGraph метатеги для социальных сетей
- Автоматическая генерация sitemap.xml
- Семантическая HTML-разметка
- Оптимизированные URL-структуры

### 4. Производительность
- Кеширование API-запросов (TTL: 5 минут)
- Lazy loading изображений
- Skeleton loaders для улучшения UX
- Оптимизация шрифтов (preload, font-display: swap)
- Минимизация bundle size

### 5. State Management
- Pinia stores для глобального состояния
- Реактивное управление избранным
- Синхронизация с localStorage
- SSR-safe реализация

## 📁 Структура проекта

```
kudago/
├── app/
│   ├── assets/
│   │   └── css/              # SCSS стили (BEM методология)
│   ├── components/           # Переиспользуемые компоненты
│   │   ├── Header.vue
│   │   ├── CitySelector.vue
│   │   ├── EventCard.vue
│   │   ├── EventCardSkeleton.vue
│   │   └── FavoriteIcon.vue
│   ├── composables/          # Композаблы (бизнес-логика)
│   │   └── useKudaGo.ts     # API клиент с кешированием
│   ├── middleware/           # Глобальные middleware
│   │   └── city.global.ts   # Валидация и редирект городов
│   ├── pages/               # File-based routing
│   │   ├── [city]/
│   │   │   ├── index.vue    # Главная страница города
│   │   │   └── events/
│   │   │       ├── index.vue # Список событий с фильтрами
│   │   │       └── [id].vue  # Детальная страница события
│   │   ├── favorites.vue    # Страница избранного
│   │   └── index.vue        # Корневая страница
│   ├── stores/              # Pinia stores
│   │   ├── city.ts         # Управление городами
│   │   └── favorites.ts    # Управление избранным
│   ├── types/              # TypeScript типы
│   └── app.vue             # Root component
├── server/
│   ├── api/                # API routes (Nitro)
│   │   ├── events.get.ts
│   │   ├── events/[id].get.ts
│   │   ├── locations.get.ts
│   │   └── __sitemap__/
│   │       └── urls.get.ts # Генерация sitemap
│   └── middleware/
│       └── location-cookie.ts
├── public/                 # Статические файлы
│   ├── fonts/             # Локальные шрифты (Roboto)
│   └── placeholder-event.svg
└── nuxt.config.ts         # Конфигурация Nuxt
```

## 🚀 Установка и запуск

### Требования
- Node.js 18+ или 20+
- npm, pnpm, yarn или bun

### Установка зависимостей

```bash
npm install
# или
pnpm install
# или
yarn install
```

### Разработка

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### Production сборка

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

### Генерация статического сайта (SSG)

```bash
npm run generate
```

## 🏗 Архитектурные решения

### 1. Композаблы (Composables)
Централизованная бизнес-логика в `useKudaGo.ts`:
- Единая точка доступа к API
- Встроенное кеширование с TTL
- Типобезопасные методы
- SSR-совместимость

```typescript
const { fetchEvents, fetchEvent, getEventImage } = useKudaGo()
```

### 2. State Management (Pinia)
Разделение состояния на логические модули:
- **city store** — управление городами и выбором
- **favorites store** — управление избранным с синхронизацией localStorage

### 3. Middleware
Глобальный middleware для валидации маршрутов:
- Проверка валидности города в URL
- Автоматический редирект на корректный маршрут
- Синхронизация cookie с URL

### 4. API Routes (Nitro)
Серверные эндпоинты для проксирования запросов:
- Абстракция от внешнего API
- Единая обработка ошибок
- Возможность добавления дополнительной логики

### 5. TypeScript
Строгая типизация для:
- API responses
- Component props
- Store state
- Composable return types

## 📊 Основные функции

### Главная страница
- Отображение ближайших событий для выбранного города
- SSR-рендеринг с готовым HTML
- Динамические метатеги

### Список событий
- Фильтрация по дате (сегодня, завтра, неделя, месяц)
- Фильтрация по категориям
- Поиск по названию
- Пагинация с индексируемыми URL
- Skeleton loaders при загрузке

### Детальная страница события
- Полная информация о событии
- OpenGraph метатеги для социальных сетей
- Добавление в избранное
- Оптимизированные изображения

### Избранное
- Сохранение в localStorage через Pinia
- Группировка по городам
- Работа без перезагрузки страницы
- SSR-совместимость

## 🔍 SEO и производительность

### SEO-оптимизация
- ✅ Server-Side Rendering всех страниц
- ✅ Динамические метатеги (title, description)
- ✅ OpenGraph для социальных сетей
- ✅ Автоматический sitemap.xml
- ✅ Семантическая HTML-разметка
- ✅ Индексируемые URL-структуры

### Производительность
- ✅ Кеширование API-запросов (5 минут TTL)
- ✅ Lazy loading изображений
- ✅ Preload критических ресурсов (шрифты)
- ✅ Skeleton loaders для улучшения perceived performance
- ✅ Оптимизация bundle size

### Кеширование
- **Client-side**: Map-based кеш в composable (5 минут)
- **Server-side**: Возможность настройки Redis через Nitro
- **Sitemap**: Кеширование на 24 часа

## 🎨 Стилизация

- **SCSS** с модульной архитектурой
- **BEM методология** для именования классов
- **CSS переменные** для темизации
- **Responsive design** (mobile-first подход)
- **Локальные шрифты** (Roboto) с оптимизацией загрузки

## 🔧 Конфигурация

### Environment Variables
Настройка API URL через `nuxt.config.ts`:

```typescript
runtimeConfig: {
  public: {
    kudagoApiUrl: 'https://kudago.com/public-api/v1.4'
  }
}
```

### Sitemap
Автоматическая генерация sitemap для:
- Главных страниц городов
- Страниц списка событий
- Детальных страниц событий (ближайшая неделя)
- Кеширование на 24 часа

## 📦 Деплой

### Рекомендуемые платформы
- **Vercel** — оптимально для Nuxt SSR
- **Netlify** — поддержка SSR и SSG
- **Cloudflare Pages** — с поддержкой Workers
- **Node.js сервер** — для полного контроля

### Build команды
```bash
# Production build
npm run build

# Static generation (если нужен SSG)
npm run generate
```

### Environment Variables
Убедитесь, что настроены:
- `NUXT_PUBLIC_KUDAGO_API_URL` (опционально, по умолчанию используется из config)

## 🧪 Тестирование

Проект готов к добавлению тестов:
- **Unit tests**: Vitest для composables и stores
- **Component tests**: Vue Test Utils
- **E2E tests**: Playwright или Cypress
- **SSR tests**: Проверка генерации HTML на сервере

## 📝 Best Practices

### Код
- ✅ TypeScript для типобезопасности
- ✅ Composition API и `<script setup>`
- ✅ Компонентный подход
- ✅ Разделение concerns (composables, stores, components)

### Производительность
- ✅ Кеширование API-запросов
- ✅ Lazy loading изображений
- ✅ Оптимизация шрифтов
- ✅ Минимизация bundle size

### SEO
- ✅ SSR для всех страниц
- ✅ Семантическая разметка
- ✅ Метатеги и OpenGraph
- ✅ Sitemap и robots.txt

### UX
- ✅ Skeleton loaders
- ✅ Обработка ошибок
- ✅ Fallback механизмы
- ✅ Responsive design

## 🔄 Roadmap

Возможные улучшения:
- [ ] Добавление unit и E2E тестов
- [ ] PWA функциональность
- [ ] Офлайн режим
- [ ] Расширенная фильтрация
- [ ] Карты для локаций событий
- [ ] Уведомления о новых событиях
- [ ] Интеграция с календарем

## 📄 Лицензия

Private project

## 👤 Автор

Проект создан для демонстрации навыков разработки production-ready приложений на Nuxt 4.

---

**Технологии**: Nuxt 4 • Vue 3 • TypeScript • Pinia • SSR • SEO
